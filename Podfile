# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FinalAdventure' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FinalAdventure
  	pod 'Firebase/Core'
  	pod 'Firebase/Auth'
  	pod 'Firebase/Database'

  target 'FinalAdventureTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FinalAdventureUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
